import 'package:flutter/material.dart';

class SavedTanksScreen extends StatelessWidget {
  static String id = 'saved_tanks_screen';

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Saved Tanks Screen'),
    );
  }
}
